let number = 10 

number++ // Operador de Incremento - number = number + 1
console.log(number)

number-- // Operador de Decremento - number = number - 1

console.log(number)
console.log(number++) // Imprime no terminal e depois soma
console.log(++number) // Soma e depois imprime no terminal

number += 10 // number = number + 10 
number -= 10 // number = number - 10 